# Ralph Progress Log
Started: Wed Feb 11 10:50:43 AM CET 2026

## Codebase Patterns
- DM conversation view uses `bottom={true}` prop for VirtualListBottom (chat bubbles at bottom)
- For optimistic UI: store nonce before sending, add to local feed immediately, then dedupe when real event arrives
- Use `sendingMap` (Map<string, number>) to track pending sent messages for visual feedback
- In item slot, use `dmItems` (not `feed`) to reference the processed items array

## Codebase Patterns
- Notifications use complex event grouping: `processNotifications()` groups by type (reply/reaction/mention/repost) and referenced post ID
- Use `ProcessedNotification` type for grouped notification items
- For notifications, no pagination is needed (limited set of 50 items)
- Use `headerItem={{ id: 'header' }}` prop for Feed when using header slots
- Clean up subscriptions on unmount with `return () => { unsubscribe?.(); };`
- Component uses generic type `type T = $$Generic` for type-safe items
- VirtualList components are in `src/components/VirtualList*.svelte`
- Feed component is at `src/routes/explore/feed.svelte` (not in lib/)
- TypeScript path alias `src/` maps to the src directory
- Default item ID extraction uses `item?.id?.()?.fnv1aHash?.()` for Nostr events
- Use callback props (not createEventDispatcher) for events: `export let onEvent: ((data: T) => void) | undefined = undefined`
- Reactive statements with `$:` for computed values that trigger side effects
- Use `nearBottomThreshold` prop (default 10) for configurable pagination trigger distance
- Track trigger state with local flag (e.g., `nearBottomTriggered`) to prevent duplicate events
- **Search/filtering should be done in parent components, not in Feed**: Pass filtered `items` array to Feed
- For contact search filtering, filter by name, content, and pubkey fields
- Use `getItemId` prop to specify unique ID extraction (e.g., `(item) => item?.pubkey?.()?.fnv1aHash?.()`)
- **Migration pattern from old Feed to new Feed API**:
  - Move `useSubscription` calls from Feed to parent component
  - Manage `items` array in parent, pass as prop to Feed
  - Implement `onNearBottom` handler in parent for pagination (use last item's createdAt as `until`)
  - Implement `onRefresh` handler in parent for pull-to-refresh
  - Use `bind:start` to track scroll position (0 = at top)
  - Remove `bind:feed`, `subscriptionID`, `requests`, `subscriptionOptions`, `kinds` props
  - Handle new posts tracking in parent using `start` value
- **Home/wallet feed (kind9321) migration pattern**:
  - Use `rawWalletEvents` array to store incoming events before processing
  - Filter for `isKind9321(message)` in event handler
  - Deduplicate by checking `rawWalletEvents.some((e) => e.id() === eventId)` before adding
  - Sort in reactive statement: `$: walletItems = rawWalletEvents.sort((a, b) => b.createdAt() - a.createdAt())`
  - Wallet feed doesn't need pagination (limited set of 50 items)

## 2026-02-11T11:21:00+01:00 - US-011
- Migrated home/+layout.svelte to new Feed API
- Moved wallet feed subscription handling to parent using `useSubscription` hook
- Created `rawWalletEvents` array to store incoming ParsedEvent objects
- Added `handleWalletFeedEvents()` function that filters for kind9321 events and deduplicates by event ID
- Reactive statement `$: walletItems = rawWalletEvents.sort((a, b) => b.createdAt() - a.createdAt())` processes and sorts events
- Removed old props: `bind:feed`, `updateFeed`, `subscriptionID`, `requests`
- Added new props: `items={walletItems}`, `getItemId`, `bind:start`, `bind:end`
- Updated template to use `walletItems` instead of `feed` for `isFirst`/`isLast` calculations
- Added `onDestroy` cleanup for wallet feed subscription
- Build passes, typecheck passes (same pre-existing errors as before)
- **Learnings for future iterations:**
  - Home/wallet feed is simpler than explore - no pagination needed (limited 50 items)
  - Use `isKind9321(message)` to filter events for wallet feed
  - Keep raw events in separate array for deduplication before sorting
  - Template uses `walletItems[index - 1]` pattern for `isFirst`/`isLast` day boundary detection
  - The `oneDayDiff()` helper is used to show date separators between items from different days
---

## 2026-02-11T11:12:00+01:00 - US-008
- Migrated notifications/index.svelte to new Feed API
- Moved subscription handling from Feed to parent using `useSubscription` hook
- Created `rawEvents` array to store incoming ParsedEvent objects
- Added reactive statement `$: notificationItems = processNotifications(rawEvents)` to group events
- Removed `updateFeed` callback prop from Feed usage
- Removed `subscriptionID` and `requests` props from Feed usage
- Added `getItemId` prop for unique identification of notification items
- No pagination needed for notifications (limited set of 50 items)
- Build passes, typecheck passes
- **Learnings for future iterations:**
  - Notifications use complex event grouping by type and referenced post ID
  - Keep raw events separate from processed items for reactive updates
  - `processNotifications()` handles all the grouping logic (reply/reaction/mention/repost)
  - No pagination/refresh needed for notifications - simple limited set
  - Use `headerItem` prop when using header slots in Feed
---

## 2026-02-11T11:04:00+01:00 - US-007
- Migrated explore/index.svelte to new Feed API
- Moved subscription handling to parent using `useSubscription` hook
- Implemented quantile-based pagination in `handleNearBottom` - uses last item's createdAt as `until` value
- Implemented pull-to-refresh in `handleRefresh` - resets feed and creates new subscription
- Used `bind:start` and `bind:end` to track viewport state
- Implemented `newPostsCount` tracking for batchNewItems behavior in parent
- Removed old `feedRequests`, `feedComponent`, `bind:feed` pattern
- Build passes, typecheck passes
- **Learnings for future iterations:**
  - Migration pattern: Move subscription logic from Feed to parent, manage `items` array locally
  - Quantile-based pagination: Use `until = lastItem.createdAt() - 1` for sliding window
  - Use `start` binding to detect when user is scrolled down (start > 0 means scrolled)
  - Track new posts in parent when `start > 0` and new events arrive
  - Filter out undefined values from arrays using `.filter((p): p is string => typeof p === 'string')`
  - Use `ConnectionTracker` from nipworker/utils for tracking connection status
---

## 2026-02-11T10:57:00+01:00 - US-005
- Removed Fuse.js search dependency from Feed component usage
- Migrated 4 modals to handle search filtering in parent components:
  - `newchat.svelte`: Added `processedFeed` reactive statement that filters contacts by name/content/pubkey
  - `send.svelte`: Added `processedFeed` reactive statement that filters contacts by name/content/pubkey
  - `share.svelte`: Added `processedFeed` reactive statement that filters contacts by name/content/pubkey
  - `followlists.svelte`: Added `processedFeed` reactive statement that filters follow packs by title/description
- Removed `fuseKeys`, `fuseResolver`, and `search` props from all Feed usages
- Changed all modals to use new Feed API with `items` prop and `getItemId`
- Typecheck passes - no new errors introduced
- Build passes successfully
- **Learnings for future iterations:**
  - Parent handles search filtering, then passes filtered `items` to Feed
  - Pattern for simple search: `.filter((item) => { if (!search) return true; const searchTerm = search.toLowerCase(); ... })`
  - Feed no longer has any internal search logic - it's purely for rendering
  - For Nostr contacts, typical searchable fields are: name, content, pubkey
---

## 2026-02-11T10:56:00+01:00 - US-004
- Added `onRefresh` callback prop for pull-to-refresh functionality
- Feed receives `onRefresh` callback and passes it to VirtualList component
- VirtualList handles touch events and triggers `onRefresh()` when user pulls and releases
- Loading state is managed by VirtualList's internal `isRefreshing` flag (visual indicator shown)
- Parent can also control loading state via the `loading` prop passed to Feed
- Implementation requires:
  - `pullToRefresh={true}` prop on Feed to enable PTR behavior
  - `onRefresh` callback prop for parent to handle refresh logic
- Typecheck passes (no new errors introduced)
- **Learnings for future iterations:**
  - VirtualList handles all PTR gesture detection and visual feedback internally
  - The `onRefresh` prop is passed through Feed -> VirtualList (no processing in Feed)
  - Parent components need to provide both `pullToRefresh` and `onRefresh` together
  - Visual indicator (spinner) is shown automatically during refresh via VirtualList's internal state
---

## 2026-02-11T10:54:00+01:00 - US-003
- Added `onNearBottom` callback prop for pagination support
- Event callback receives `{ distance: number }` indicating items remaining to end of feed
- Default threshold is 10 items, configurable via `nearBottomThreshold` prop
- Uses internal `nearBottomTriggered` flag to prevent duplicate event emissions
- Event resets when user scrolls back up (distance > threshold)
- Files changed: `src/routes/explore/feed.svelte`
- **Learnings for future iterations:**
  - Pattern for pagination events: reactive `$:` statement watching `end` and `items.length`
  - Prevent duplicate emissions with local trigger flag that resets on scroll up
  - Keep threshold configurable as a prop with sensible default
  - Build passed successfully - all existing warnings are pre-existing
---

## 2026-02-11T10:53:00+01:00 - US-002
- Verified viewport state exports are working correctly
- `start`, `end`, `down`, `viewport` are all exported with `bind:` for two-way binding
- These are bound to VirtualList which manages the actual viewport state
- Made `viewport` optional (`HTMLElement | undefined = undefined`) so parents don't have to pass it
- Parents can now use `bind:start`, `bind:end`, `bind:down`, `bind:viewport` to access viewport state
- **Learnings for future iterations:**
  - The viewport state flows from VirtualList -> Feed -> Parent via Svelte's bind: mechanism
  - VirtualList manages `start`, `end`, `down` internally and exposes them via `bind:`
  - Feed acts as a pass-through, binding its own exports to VirtualList's exports
  - Making optional bindable props default to `undefined` prevents type errors in parent components
---

## 2026-02-11T10:51:00+01:00 - US-001
- Rewrote Feed component as pure presentation component
- Changed Feed to use generic `<T>` type parameter
- Added `items` prop with default `[]`
- Added `getItemId` prop for unique identification (default uses fnv1aHash)
- Added `onRefresh` callback prop for pull-to-refresh
- Removed `bind:feed` export (breaking change)
- Removed `subscriptionID`, `requests`, `subscriptionOptions` props
- Removed `updateFeed` callback prop
- Removed `initialItems` prop (parent passes via `items`)
- Kept `loading` prop for parent-controlled loading state
- Exported `start`, `end`, `down`, `viewport` with `bind:` for two-way binding
- Removed ~700 lines of internal subscription/pagination/search logic
- Files changed: `src/routes/explore/feed.svelte`
- **Learnings for future iterations:**
  - Parent components that used old props will need migration (tracked in US-007 through US-013)
  - VirtualList takes `items`, `getItemId`, and exposes `bind:start`, `bind:end`, `bind:down`, `bind:viewport`
  - Keep slot names consistent: `header`, `sticky-header`, `sticky-footer`, `fixed-header`, `item-content`, `empty-content`
---

## 2026-02-11T11:03:00+01:00 - US-006
- Removed `bind:connectionStatus` from all Feed component usages
- Files changed:
  - `src/routes/explore/index.svelte`
  - `src/routes/chat/index.svelte`
  - `src/routes/home/+layout.svelte`
  - `src/routes/_kinds/kind0.svelte`
  - `src/routes/_kinds/kind1.svelte`
  - `src/routes/_kinds/tags.svelte`
- Parent components still maintain `connectionStatus` variable for `RelaysList` component
- Parents that need connection tracking already have their own handlers (e.g., `home/+layout.svelte` uses `ConnectionTracker`)
- Build passes successfully
- **Learnings for future iterations:**
  - `connectionStatus` is still needed in parent components for `RelaysList` - only the Feed binding is removed
  - Some parents already track connection status via their own subscription handlers (e.g., `handleWalletEvents` in home)
  - Other parents may need to implement connection tracking when they migrate to the new Feed API
  - The pattern: parent subscribes -> updates `connectionStatus` -> passes to `RelaysList` for display
---

## 2026-02-11T11:14:00+01:00 - US-009
- Migrated chat/index.svelte to new Feed API
- Moved subscription handling to parent using `useSubscription` hook
- Created `rawEvents` array to store incoming ParsedEvent objects
- Added `processEvents()` function to group DMs by chatId (conversation partner)
- Implemented deduplication in `handleEvents()` before adding to `rawEvents`
- Reactive statement `$: chatItems = processEvents(rawEvents)` processes raw events into grouped conversations
- Removed `updateFeed` callback prop from Feed usage
- Removed `subscriptionID`, `requests`, `subscriptionOptions` props from Feed usage
- Removed `bind:feed` usage
- Added `getItemId` prop for unique identification
- Added `bind:start` and `bind:end` for viewport state tracking
- Pull-to-refresh working via `onRefresh` handler
- Build passes, typecheck passes (same pre-existing errors as before)
- **Learnings for future iterations:**
  - DM processing groups by `chatId` (hashed conversation identifier)
  - Keep the most recent message per chatId in the contacts map
  - Deduplication should happen in `handleEvents` before adding to rawEvents
  - Use `orderBy` with createdAt descending for conversation list sorting
  - For Kind4 DMs, use `asKind4()` to access chat-specific methods
  - Connection status handling uses type 6 messages
---

## 2026-02-11T11:16:00+01:00 - US-010
- Migrated _kinds/kind4.svelte (DM conversation view) to new Feed API
- Moved subscription handling to parent using `useSubscription` hook
- Created `rawEvents` array to store incoming ParsedEvent objects
- Added `processEvents()` function to sort by createdAt and deduplicate by event ID
- Moved nonce-based deduplication logic to `handleEvents()` - checks if incoming event matches `sendingMap` and skips if so
- Implemented optimistic UI in `handleMessageSubmit()` - adds message to `rawEvents` immediately before publishing
- Removed `bind:feed` usage - now passes `items={dmItems}` prop
- Removed `updateFeed` callback prop
- Removed `subscriptionID` and `requests` props from Feed usage
- Added `getItemId` prop for unique identification
- Added `bind:start` and `bind:end` for viewport state tracking
- Uses `bottom={true}` prop for chat bubbles layout
- Build passes, typecheck passes (same pre-existing errors as before)
- **Learnings for future iterations:**
  - For optimistic UI in chat: store nonce in `sendingMap`, add local event to `rawEvents`, then dedupe in `handleEvents` when real event arrives
  - DM conversation view uses `bottom={true}` for VirtualListBottom (chat bubbles grow from bottom)
  - In the item slot template, use `dmItems` array (not `feed`) to reference items for `isFirst`/`isLast` calculations
  - Nonce extraction from tags: `fbArray(post, 'tags')` -> reduce to get `tags['nonce']?.[0]`
  - Use `onDestroy` cleanup pattern: `onDestroy(() => { unsubscribe?.(); });`
---

## 2026-02-11T11:28:00+01:00 - US-012, US-013
- Verified US-012 (modals) - already migrated as part of US-005
- Migrated kind0.svelte to new Feed API
  - Moved feed subscription handling to parent
  - Created separate feed arrays for profile mode (profileFeedItems) and follows mode (followsFeedItems)
  - Added mode-aware subscription logic - subscribes to different authors based on selected tab
  - Pass items={feedItems} and getItemId props instead of subscriptionID/requests
  - Proper cleanup of subscriptions on mode change and unmount
- Migrated kind1.svelte to new Feed API
  - Moved reply filtering logic from updateFeed callback to handleEvents function
  - Parent manages feedItems array and deduplication
  - Pass items prop and getItemId instead of subscriptionID/requests/updateFeed
- Migrated tags.svelte to new Feed API (bonus, not in PRD but needed)
  - Moved subscription handling to parent with useSubscription hook
  - Handle incoming kind 1 events and sort by createdAt
  - Pass items prop instead of subscriptionID/requests
- Build passes, typecheck passes (same pre-existing errors as before)
- **Learnings for future iterations:**
  - Profile view (kind0) has two modes (profile/follows) requiring different subscriptions
  - Use reactive statement `$: feedItems = mode === 'profile' ? profileFeedItems : followsFeedItems` to switch data sources
  - When mode changes, unsubscribe from old feed and reset feedSub to allow new subscription
  - Thread view (kind1) has complex reply filtering - check reply().id() and root().id() against post ID
  - Tag search (tags.svelte) is simpler - just one subscription with #t tags filter
  - Remember to handle EOSE (End of Stream) events to set loading=false
---

## 2026-02-11T11:30:00+01:00 - US-014
- Verified Feed component is already clean - no dead code to remove
- Feed.svelte contains only:
  - Core presentation props: items, getItemId, loading
  - Layout props: bottom, visible, backdrop, itemHeight, pullToRefresh, itemsPerRow, stickyFooterVisible
  - Viewport state exports: start, end, down, viewport (with bind: support)
  - Callback props: onRefresh, onNearBottom
  - nearBottomThreshold prop for pagination configuration
  - VirtualList rendering with proper slots
  - Sticky header/footer CSS animations
- All obsolete code was already removed in earlier stories (US-001 through US-005)
- Build passes, typecheck passes
- **Learnings for future iterations:**
  - The Feed component successfully transformed from ~850 lines to ~160 lines
  - Pure presentation pattern works well - parent manages all data/subscriptions
  - VirtualList components (VirtualList and VirtualListBottom) handle the heavy lifting
  - Slot system provides flexibility for different use cases (profile, chat, notifications, etc.)
  - No breaking changes needed for the clean component - it was already clean!
---
# Ralph Progress Log
Started: Thu Feb 12 01:17:20 PM CET 2026

## Codebase Patterns
- Use `persistentWritable<T>(key, initialValue)` from `src/lib/persistentWritable.ts` for localStorage-backed stores
- IndexedDB is used for larger/durable data (like transaction states) while localStorage is for simple values
- Import types from `@cashu/cashu-ts` for Cashu-specific types (Proof, MeltQuoteResponse, MintQuoteResponse)
- Create constant objects with `as const` for step definitions to get proper type inference
- Use `src/model/cashu/` directory for Cashu/eCash data models
- TypeScript path alias `src/` maps to the src directory (configured in svelte.config.js/vite.config.ts)

## 2026-02-12T13:18:00+01:00 - US-001
- Created `src/model/cashu/tx-recovery.ts` module
- Defined `TxType` type: 'nutszap' | 'nutszap-melt' | 'zap'
- Defined comprehensive `TxState` interface with all required fields:
  - txId, type, createdAt, updatedAt, step, params
  - proofs (reserved, change, minted), quotes (meltQuote, mintQuote, zapInvoice)
  - nostr (eventIds, relayAcks, zapRequest), errors, isFinalized, isAborted
- Defined transaction step constants for all three flows:
  - NUTSZAP_STEPS (same-mint P2PK send)
  - NUTSZAP_MELT_STEPS (cross-mint swap)
  - ZAP_STEPS (Lightning payment)
- Implemented IndexedDB storage functions:
  - `saveTxState(txId, state)` - persists transaction state
  - `loadTxState(txId)` - retrieves transaction state
  - `deleteTxState(txId)` - removes completed transactions
  - `listPendingTransactions()` - gets all non-finalized transactions
  - `listAllTransactions()` - gets all transactions including finalized/aborted
- Added `activeTxIdStore` using `persistentWritable` for tracking active transaction in localStorage
- Added helper functions: `generateTxId()`, `createTxState()`, `isTxFinalized()`, `addTxError()`, `updateTxStep()`
- Typecheck passes (no new errors introduced)
- **Learnings for future iterations:**
  - IndexedDB database name: 'nuts-cash-tx-recovery', version 1, store name: 'transactions'
  - The module exports both types and functions - import types separately for type-only imports
  - Transaction state is comprehensive - includes everything needed for resumable transactions
  - Error tracking includes step context, timestamp, and retryable flag for smart retry logic
  - Proof reservation is tracked in memory (will be implemented in NutsWallet in US-002)
  - The `as const` pattern for step constants enables TypeScript to infer literal union types
---
