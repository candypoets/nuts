--- a/src/routes/_kinds/kind1.svelte
+++ b/src/routes/_kinds/kind1.svelte
@@ -153,7 +153,10 @@
 									relays
 									}
 								],
-								handleEvents
+								handleEvents,
+								{
+									pipeline: $defaultPipeline.for('replies_' + data?.id)
+								}
 							);
 						}
 					},
